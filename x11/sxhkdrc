XF86Audio{Raise,Lower}Volume
    amixer sset Master {1%+, 1%-} unmute && notify-send -h int:value:$(amixer sget Master | grep -Pom1 '\d*%') "Volume"
XF86AudioMute
    amixer sset Master toggle && notify-send "$(amixer sget Master | grep -Pom1 '\[(on|off)\]') Volume"

alt+t
    $TERMINAL
alt+e
    rofi -rnow
alt+{p,r}
    xfce4-appfinder { ,-c}
alt+Print
    xfce4-screenshooter
alt+f
    perevod call fetch
alt+ctrl+l
    i3lock -i ~/lock-s.png
alt+ctrl+b
    chromium
alt+ctrl+h
    $TERMINAL evilvte -T htop -e htop

alt+s; {r,q,s}
    systemctl {reboot,poweroff,suspend}

alt+i; {i,l,k,s}
    internet { ,light,kill,skype}

alt+a; {a,w,s,d,r,q}
    tider call {target,menu,report,disable,reload,quit}
