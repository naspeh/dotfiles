#!/usr/bin/env python
import argparse
import datetime as dt
import os
import socket
import subprocess
import sys

BDIR = '/arch/backup/'
RSYNC = 'sudo rsync -aAXHvhy --stats --delete'
FILTERS = [
    '+ /boot/',
    '+ /etc/',
    '+ /root/',
    '+ /home/',
    '+ /home/naspeh/',
    '+ /home/naspeh/v/',
    '- /home/naspeh/v/*',
    '- /home/**/.thumbnails/',
    '- /home/*',
    '+ /arch/',
    '+ /arch/naspeh/',
    '+ /arch/nayavu/',
    '+ /arch/photo/',
    '- /arch/*',
    '- /**/.cache/',
    '- /**/__pycache__/',
    '- /**/*.pyc',
    '- /*'
]
TARGETS = {
    'pad_to_box': 'backup copy box:/arch/backups/pad/ --nolog --extra="-z"',
    'box_to_sea': (
        'backup copy /arch/ /media/sea/ --nolog --extra="'
        '   --exclude={/lost+found,/.delta,/backup}'
        '   --backup --backup-dir=/media/sea/.delta/'
        '"'
    )
}


def main():
    parser = argparse.ArgumentParser()
    subs = parser.add_subparsers()

    def sub(name, **kw):
        s = subs.add_parser(name, **kw)
        s.set_defaults(sub=name)
        s.arg = lambda *a, **kw: s.add_argument(*a, **kw) and s

        s.arg('--log', action='store_true', help='log to file')
        s.arg('--nolog', action='store_true', help='don\'t log to file')
        s.arg('--bdir', default=BDIR, help='backup directory')
        s.arg_fake = lambda: (
            s.arg('-f', '--fake', action='store_true', help='dry run')
        )
        return s

    sub('run', help='backup important data').arg_fake()

    sub('tar', help='archive delta directory')\
        .arg('-l', '--label', help='label for archive')\
        .arg('-d', '--delete', help='delete delta directory')

    sub('copy', help='copy backup directory').arg_fake()\
        .arg('src', default=BDIR, nargs='?', help='source path')\
        .arg('dest', help='destination path')\
        .arg('-e', '--extra', default='', help='extra options for rsync')

    sub('full', help='backup whole root partition').arg_fake()

    sub('call', help='call fixed action')\
        .arg('target', choices=TARGETS.keys(), help='choice fixed action')

    args = parser.parse_args(sys.argv[1:])
    if not hasattr(args, 'sub'):
        parser.print_usage()
        exit(2)

    today = dt.date.today().isoformat()
    now = dt.datetime.now().isoformat()
    if hasattr(args, 'fake'):
        rsync = RSYNC + ' --dry-run' if args.fake else RSYNC
    bdir = args.bdir.rstrip('/') + '/'
    pkg, log, mnt = bdir + 'pkg/', bdir + 'log/', bdir + 'mnt/'
    for f in [log, pkg, mnt]:
        if not os.path.exists(f):
            os.mkdir(f)

    def sh(cmd, **kw):
        cmd = 'echo "{}" && time {}'.format(cmd.replace('"', '\"'), cmd)
        use_log = args.log or os.environ.get('BACKUP_LOG', False)
        if not args.nolog and use_log:
            file = '%s%s.log' % (log, now)
            cmd = '({}; echo "{}") &>> {}'.format(cmd, '-' * 20, file)
        subprocess.call(cmd, shell=True, **kw)

    if args.sub == 'run':
        sh(
            'pkglist -n > {0}pkglist.txt'
            ' && pkglist -m > {0}pkgaur.txt'
            .format(pkg)
        )

        rsync += ' --delete-excluded ' + (
            ' '.join(['--filter="%s"' % v for v in FILTERS])
        )
        sh(
            rsync +
            ' --backup --backup-dir={dest}delta/'
            ' / {dest}latest/'
            .format(dest=bdir)
        )

    elif args.sub == 'tar':
        name = today
        if args.label:
            name += '-' + args.label
        sh(
            'cd {dest} && tar -cvf {name}.tar --directory=delta .'
            .format(dest=bdir, name=name)
        )
        if args.delete:
            sh('rm -r {}delta'.format(bdir))

    elif args.sub == 'copy':
        sh(
            rsync + ' -x {extra} {src} {dest}/'
            .format(src=args.src, dest=args.dest.rstrip('/'), extra=args.extra)
        )

    elif args.sub == 'full':
        snap = '/dev/%s/snap' % socket.gethostname()
        root = '/dev/%s/root' % socket.gethostname()
        rsync += ' -x --exclude=' + (
            '{/dev/*,/proc/*,/sys/*,/tmp/*,/run/*,/mnt/*,/media/*,/lost+found}'
        )
        try:
            sh(
                'lvcreate --size 10G --snapshot --name snap {root}'
                ' && mount {snap} {mnt}'
                ' && {rsync} {mnt} {dest}full/'
                .format(
                    dest=bdir, rsync=rsync,
                    root=root, snap=snap, mnt=mnt
                )
            )
        finally:
            sh(
                'umount {mnt}; lvremove -f {snap}'
                .format(mnt=mnt, snap=snap)
            )

    elif args.sub == 'call':
        cmd = TARGETS.get(args.target)
        if args.target.startswith(socket.gethostname()):
            sh(cmd)
        else:
            print(cmd)
            raise SystemExit('...Wrong hostname for this target.')


if __name__ == '__main__':
    try:
        main()
    except KeyboardInterrupt:
        exit('Closed.')
