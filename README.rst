Shell and X11 files
-------------------

Installation:
    .. code-block:: sh

        $ ./manage.py init --boot vim zsh
        # Or dev and shell targets
        $ ./manage.py init --boot all-shell dev
